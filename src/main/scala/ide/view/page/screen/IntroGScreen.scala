package az.rock.ide.view.page.screen

import az.rock.ide.view.ui.frame.GMainFrame

class IntroGScreen() extends GMainFrame("RockTadpole-IDEA") {
  private[screen] val STATE = null

  override def postConst() = {
  }

  override def initializer() = {
  }
}