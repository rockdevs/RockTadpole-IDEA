package az.rock.ide.view.page.screen.toolBar.file.newFile;

import az.rock.ide.view.page.lib.panel.GPanel;

public abstract class InnerQueuePanel extends GPanel implements Comparable<InnerQueuePanel> {
    @Override
    public int compareTo(InnerQueuePanel o) {
        return 0;
    }
}
